spring.application.name=WelZone App

server.port=${PORT:8080}

# MySQL connection settings (override via DB_URL / DB_USERNAME / DB_PASSWORD on the host)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/welzoneapp}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:@Pratham1}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT settings (override via JWT_SECRET / JWT_EXPIRATION_MS on the host)
jwt.secret=${JWT_SECRET:WelZoneDbmsCourseProjectSecretKeyForJwtSigning2026@Secure}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

## JPA settings
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.generate-ddl=true
#spring.jpa.show-sql=true
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
